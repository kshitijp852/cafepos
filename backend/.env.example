# MongoDB Connection
MONGO_URL=mongodb://localhost:27017
DB_NAME=pos_database

# CORS Configuration
CORS_ORIGINS=*

# JWT Configuration (use a strong random string in production)
JWT_SECRET=your-super-secret-key-change-this-in-production-use-a-long-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=24
